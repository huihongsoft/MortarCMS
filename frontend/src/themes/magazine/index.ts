import Header from './Header';
import Footer from './Footer';
import HomeLayout from './HomeLayout';

// Magazine theme entry (bundled as themes/magazine.js at build time)
export default { name: 'magazine', Header, Footer, HomeLayout };
